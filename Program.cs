using System;

namespace Algorithms
{
    class Program
    {
       
        public static void Main(string[] args)
        {
            //Utility.Display();
            //Utility.Anagram("CAT", "shubham");

            //Utility.PrimeRange();
            //Utility.Palindome(55);
            /*
            int[] arr = { 25, 14, 13, 58, 96 };
            Console.WriteLine(Utility.BinarySearch(arr,13,0,4));
            */
            /*
            string[] arr = { "Ram", "Shyam", "shubh", "Bro" };
            string x = "Bro";
            Console.WriteLine(Utility.BinaryString(arr, x));
            */
            //Utility.VendingMachine(12585);

            /*
            Utility.Vending obj = new Utility.Vending();
            Console.WriteLine("Enter the note");
            int note =Convert.ToInt32(Console.ReadLine());
            obj.check(note);
            */
            Utility.InsertionNumber();

        }
    }
}
